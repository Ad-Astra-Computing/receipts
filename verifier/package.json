{
  "name": "receipts-verifier",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "description": "Public, client-side verifier for Folio receipts bundles (receiptsofthought.com)",
  "license": "Apache-2.0",
  "repository": "github:Ad-Astra-Computing/receipts",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "test:produce": "vitest run src/produce.test.ts"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
