{
  "name": "receipts-verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public, client-side verifier for Folio receipts bundles (receiptsofthought.com)",
  "license": "Apache-2.0",
  "repository": "github:Ad-Astra-Computing/receipts",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "deploy": "wrangler pages deploy dist --project-name=receiptsofthought --branch=main"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
